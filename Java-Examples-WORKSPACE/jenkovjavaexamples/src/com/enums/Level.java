package com.enums;

public enum Level {
	HIGH, MEDIUM, LOW
}

/*
 * A Java Enum is a special Java type used to define collections of constants.
 * More precisely, a Java enum type is a special kind of Java class. An enum can
 * contain constants, methods etc. Java enums were added in Java 5.
 */